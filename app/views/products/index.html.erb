<h1>Products</h1>

<!-- Nav -->
<%= link_to "New Product", new_product_path %>


<% @products.each do |product| %>
	<p><%= link_to "Product", "/products/#{product.id}" %>: <%= product[:name] %>: <%= product[:price_in_cents] %></p>
	<p >Description: <%= product[:description] %></p>
<% end %>
